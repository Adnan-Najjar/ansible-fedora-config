# True color settings
set -g default-terminal "$TERM"
set -ag terminal-overrides ",$TERM:Tc"
set-option -g focus-events on
set -g prefix ^S

bind-key "e" display-popup -E -w 50% "$HOME/.local/bin/tmux-sessionizer.sh"

set-option -g status-style bg=#000000,fg=colour252
